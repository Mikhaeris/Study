#include <iostream>
#include <string>

using namespace std;

string findMaxPalindrom(string str) {
	for
}

int main() {
	string str = "";

	cin >> str;
	string maxPalindrom = findMaxPalindrom(str);
	cout << maxPalindrom << endl;

	return 0;
}